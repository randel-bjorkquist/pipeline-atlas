namespace PipelineAtlas.Core;

// Bumped alongside the package version. Stamped into manifest.target so a manifest
// records which engine produced it. The CLI can override at call time.
public static class ToolInfo
{
    public const string Version = "0.1.0";
}
